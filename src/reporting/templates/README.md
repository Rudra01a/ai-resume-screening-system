# reporting/templates/

Jinja2 HTML templates for richer report formats (e.g. an HTML report
rendered to PDF via a headless browser) can live here as the reporting
module grows beyond the current fpdf2-based plain PDF output.
