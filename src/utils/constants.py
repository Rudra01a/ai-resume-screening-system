"""
Project-wide constants that aren't tunable config (see config/config.yaml
for tunable values) — things like fixed labels, default filenames.
"""

SUPPORTED_RESUME_EXTENSIONS = (".pdf", ".docx")

SECTION_NAMES = ("header", "education", "experience", "skills", "projects", "certifications")

DEFAULT_TOP_K = 20
