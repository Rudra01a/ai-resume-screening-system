# API Reference

This file will document the public function signature of every `src/`
module as it's implemented. Populate incrementally — one section per
module, added in the same order the module is built (see "Implementation
Order" in the project README / initial planning doc).

## Template for each entry

```
### module.function_name(args) -> return_type
Short description.
Args: ...
Returns: ...
```

_(Left intentionally sparse — fill in as each module in src/ stabilizes,
rather than maintaining hand-written docs that drift from the code.)_
