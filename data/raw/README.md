# data/raw/

Immutable inputs only. Drop resumes into `resumes/` and job description
files into `job_descriptions/`. Nothing in this folder should ever be
edited or overwritten by the pipeline — treat it as read-only source data.
